A quick and simple React app to generate a markdown changelog for a modpack based on a JSON input of mod changes.

Export Modlist JSON from Prism Launcher or MultiMC with the following format:

```json
{
    "name":"{name}",
    "url":"{url}",
    "version":"{version}"
}
```
